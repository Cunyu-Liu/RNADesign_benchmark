{
  "benchmark": "ToeholdDesignBench",
  "version": "0.2",
  "task": "R1_canonical_target_ranking",
  "label_semantics": "canonical signed ON minus OFF",
  "primary_metric": "success_at_1",
  "secondary_metrics": ["success_at_3", "success_at_5", "ndcg_at_10", "normalized_regret_at_10", "pareto_front_coverage_at_10"],
  "auxiliary_metrics": ["pooled_spearman_rho"],
  "submission": {
    "format": "csv",
    "columns": ["record_id", "score"],
    "budget_K": [1, 3, 5],
    "validation": "exactly one finite score for every test record_id"
  },
  "splits": {
    "default": "source_disjoint",
    "seed": 0,
    "train_val_test_ratio": [0.7, 0.15, 0.15],
    "invariant": "target_id never crosses train/val/test"
  },
  "information_regimes": ["local_sequence", "local_sequence_plus_biophysical"],
  "statistics": {
    "unit": "target_id",
    "ci": "95% target-level bootstrap",
    "multiple_testing": "paired bootstrap deltas; primary metric pre-registered"
  },
  "target_accounting": ["n_targets", "n_targets_with_feasible_candidate", "n_targets_without_feasible_candidate"],
  "blinding": "test labels (ON/OFF) hidden from method fitting and selection; success thresholds pre-registered",
  "separate_tracks": {
    "R2_BEACON": "BEACON-normalized ON_OFF; source-disjoint mixed and TF-to-virus domain OOD; rank metrics only",
    "R3_VISTA": "single-target paired truncated/full context stress test; no cross-target CI"
  }
}
